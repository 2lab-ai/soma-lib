{
  "name": "soma-lib",
  "version": "0.3.1",
  "description": "Shared hexagonal core for soma (Telegram/Slack personal bot) and soma-work (Slack work harness): domain logic, ports, and adapters extracted step by step.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/2lab-ai/soma-lib.git"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "bun": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "TZ=Asia/Seoul vitest run",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run test",
    "release:pack": "npm run check && npm run build && npm pack"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
